kubectl create secret generic employee-secret --from-literal=MYSQL_ROOT_PASSWORD=Admin123 \
    --from-literal=MYSQL_USER=employee_admin --from-literal=MYSQL_PASSWORD=User123 \
    --from-literal=MYSQL_DATABASE=employee_management

kubectl create namespace demo-ns


To update the image for an deployment
kubectl set image -n <namespace> deployment/<deployment-name> <container-name>=<image-name>:<tag>

kubectl rollout status deployment <deployment-name> -n <name-space>

kubectl rollout history deployment <deployment-name> -n <name-space>
